<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.shareloc">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".View.AchievedService.AchievedServiceDetails"></activity>
        <activity android:name=".View.Service.ServiceDetails" />
        <activity android:name=".View.Service.AddServiceForm" />
        <activity
            android:name=".View.Colocation.ColocationDetails"
            android:windowSoftInputMode="adjustPan" />
        <activity android:name=".View.SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".View.Authentication.AuthActivity" />
        <activity android:name=".View.MainActivity" />
    </application>

</manifest>